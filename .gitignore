# Frontend
frontend/node_modules/
frontend/dist/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Secrets
.env
backend/.env
.vercel

# OS / editors
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local OpenVINO artifacts
backend/local_scoring/models/
backend/local_scoring/cache/

# Self-recorded calibration/evaluation clips (raw video only; labels + reports stay tracked)
backend/local_scoring/eval_data/clips/
backend/local_scoring/eval_reports/*.json
backend/local_scoring/eval_reports/*.md
backend/app/eval_reports/*.json
backend/app/eval_reports/*.md

# Stray OpenVINO compiled-model cache. LOCAL_CACHE_DIR was resolving to the
# process working directory when left blank; fixed in app/local_delivery.py,
# ignored here so an older checkout or a custom LOCAL_CACHE_DIR cannot commit
# multi-megabyte .blob files.
runtime/
*.blob

# Local tooling state
.claude/scheduled_tasks.lock
