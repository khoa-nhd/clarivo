# Public UI cleanup

- Removed `Content + delivery + drills ready` / `Content + drills ready` from the top bar.
- Removed `Present · drill · understand` from the top bar.
- Removed the small `+` button from the Session queue header.
- Kept the primary `+ New session` button in the top-right corner.
- Simplified the top bar to a two-column Brand / New session layout.
- Kept Phase 20 Audio/Vision, Topic Library refresh, isolated Q&A scoring, same-type follow-ups, Overall after Q&A, and the rest of Learning Loop v2 unchanged.
